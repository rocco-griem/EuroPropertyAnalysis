"""Pure, dependency-light calculation functions (returns, risk, affordability).

These functions take plain numbers / sequences and return plain numbers / lists. They have no
knowledge of pandas, the database, or files, which keeps them trivial to unit-test and reuse.
"""
